#include "config.h"
#include "camera.h"

// coords begin at the left bottom corner
namespace camera
{

    glm::vec2 project(glm::vec2 vec, float_t z)
    {
        return glm::vec2{

        };
    }
}
